import React from 'react';

const Footer = (props) => {
    return (
        <footer className="c-footer">
            <p>Heroes manager</p>
        </footer>
    );
};

Footer.defaultProps = {
    title: 'Footer'
};

export default Footer;
